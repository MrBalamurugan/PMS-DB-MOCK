{
  "name": "db-mock",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
